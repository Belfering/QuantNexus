# Local Development - Use Local API Server
# This configuration is for pure local development where both
# frontend and backend run on the same computer

# API Base URL - Local API Server
VITE_API_URL=http://localhost:8787

# To use this configuration:
# 1. Start local API server: npm run api
# 2. Start frontend: npm run dev
# 3. Both will use the local SQLite database
